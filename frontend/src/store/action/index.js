export const changedColorAction = (item) => ({
  type: 'CHANGED_COLOR',
  item
})

export const setColorAction = (item) => ({
  type: 'SET_COLOR',
  item
})

export const signinAcition = (item) => ({
  type: 'SIGNIN',
  item
})
